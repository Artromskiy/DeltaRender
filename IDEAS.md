# DeltaRender ideas

Not active work:

- Persistent mapped staging and dirty-range uploads after measured upload cost.
- Render-graph/resource scheduling after graphics, UI and compute share enough
  real lifetime cases.
- Multi-window and editor viewport composition after the single-window path is
stable on macOS, Windows and Linux.
