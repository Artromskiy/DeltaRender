# DeltaRender ideas

Not active work:

- Persistent mapped staging after measured upload cost; the required staging
  arena itself is part of the current graph implementation.
- Multi-window and editor viewport composition after the single-window path is
  stable on macOS, Windows and Linux.
